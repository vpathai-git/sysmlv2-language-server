# Grammar Package Build Artifacts
# These are regenerated by: npm run build (or: npx langium generate && tsc)

# Generated parser and AST (from langium generate)
# Note: Already in root .gitignore as sysml_server/packages/grammar/src/generated/
# but keeping here for clarity

# TypeScript build output
dist/
*.tsbuildinfo

# Development test files
test-*.sysml

# Node modules (already in root .gitignore)
node_modules/

# IDE
.vscode/
*.code-workspace

# Temporary files
*.tmp
*.bak
*.swp
*~

# OS files
.DS_Store
Thumbs.db
